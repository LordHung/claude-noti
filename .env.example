# Copy this file to `.env` and fill in your bot details.
# The `make setup-telegram` target reads these values, then writes
# ~/.claude/hooks/telegram.env (with a chat_id auto-discovered via getUpdates).
#
# Get a token by talking to @BotFather on Telegram.

TELEGRAM_TOKEN=
TELEGRAM_URL=https://api.telegram.org
